import 'package:flutter/material.dart';

class ScanOutPage extends StatelessWidget {
  const ScanOutPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Scan Out Page'),
    );
  }
}
